# 跑步助手应用配置文件
# 本文件包含应用的所有配置选项
# 请根据实际需求修改相应的配置项

# ============================================================================
# SSL/HTTPS 配置节
# 用于配置应用的HTTPS支持和SSL证书
# ============================================================================
[SSL]
# 是否启用HTTPS（SSL/TLS）支持
# true = 启用HTTPS，false = 使用HTTP
# 默认值：false（向后兼容，默认不启用）
ssl_enabled = false

# SSL证书文件路径（PEM格式）
# 这是服务器的公钥证书，包含服务器的身份信息
# 通常由证书颁发机构（CA）签发，文件扩展名为.pem或.crt
# 可以使用绝对路径或相对于程序根目录的相对路径
# 示例：ssl/fullchain.pem 或 /etc/ssl/certs/fullchain.pem
ssl_cert_path = ssl/fullchain.pem

# SSL私钥文件路径（PEM格式）
# 这是与证书对应的私钥文件，必须严格保密
# 文件扩展名通常为.key或.pem
# 可以使用绝对路径或相对于程序根目录的相对路径
# 示例：ssl/privkey.key 或 /etc/ssl/private/privkey.key
ssl_key_path = ssl/privkey.key

# 是否强制使用HTTPS（仅HTTPS模式）
# true = 自动将所有HTTP请求重定向到HTTPS
# false = 允许HTTP和HTTPS共存
# 注意：此选项仅在ssl_enabled=true时生效
# 默认值：false（允许HTTP访问）
https_only = false

# ============================================================================
# 其他配置节可以在此添加
# 例如：数据库配置、日志配置、应用配置等
# ============================================================================
